# Rebrand to Solboards (2026-05-03) ✅

**Status:** ✅ Shipped. Frontend tested in browser, typecheck clean, working tree at 19 modified + 1 added + 1 deleted.

**Trigger:** domain decided — **solboards.xyz** (registered, DNS on Cloudflare). The earlier "x402 Ad Server" working name was always provisional; locking in the public brand before the GCE deploy in Session 18 means we don't ship the wrong name to a public URL.

**Brand vs protocol distinction (the rule going forward):**
- **Solboards** = our product, network, UI copy, container names, README headings, commit messages.
- **x402** = the actual HTTP 402 payment protocol we're built on. Stays everywhere it's a protocol reference: `x402-solana` package, `createX402Client`, "Signing x402 payment", `proof_context` JWT, "x402 protocol fee", etc.

---

## What shipped

### Frontend — UI copy + logo component
- `frontend/index.html` — `<title>` → "Solboards — Advertiser Dashboard"
- `frontend/src/components/AppHeader.tsx` — header text "x402" → "solboards", subtitle "DOOH ad protocol" → "DOOH ad network" (we're a platform built on the protocol, not the protocol)
- `frontend/src/pages/Login.tsx` — heading + footer copy
- `frontend/src/pages/Overview.tsx` — "x402 network" → "Solboards network" (×2, this is our ad network not the protocol)
- **Logo component renamed**: `frontend/src/components/ui/X402Mark.tsx` → `BrandMark.tsx`. SVG gradient id `x402-grad` → `brand-grad`. Two import sites updated (`AppHeader.tsx`, `Login.tsx`). Kept the same purple→teal gradient X-mark visual.

### Frontend — Privy modal theming (bundled into the rebrand)
- `frontend/src/main.tsx` — added `appearance` config: `theme: "#0E1118"` (matches `--bg-1`), `accentColor: "#3D5AFE"` (matches `--x402-blue`, kept as a CSS token), `logo` as inline data-URI of the BrandMark SVG (no hosted asset needed), `landingHeader: "Sign in to Solboards"`, `loginMessage: "On-chain DOOH ad campaigns on Solana"`. Privy's modal now reads as ours; emails still need the dashboard-side rename (see "Privy email" below).

### Backend
- `backend/.env` + `.env.example` — `APP_NAME="Solboards"`
- `backend/app/config.py` — `app_name = "Solboards"`
- `backend/scripts/e2e_demo.py` — print banner

### Containers (cosmetic but visible in `docker ps`)
- `docker-compose.yml` — `solboards-backend`, `solboards-frontend`
- `docker-compose.prod.yml` — `solboards-backend`, `solboards-caddy`
- `frontend/package.json` + `package-lock.json` — `name: "solboards-frontend"`

⚠ **Heads-up:** any local containers from before this change are named `x402-adserver-*` and will be orphaned. Before next `up`, run `docker compose down` so the new container names take cleanly.

### Docs
- Headings in `PLAN.md`, `BACKGROUND-INFORMATION.md`, `DESIGN-BRIEF.md` (×2 — title + first line), `BACKEND-REVIEW.md` (JWT `iss` recommendation now reads `iss="solboards"` for whoever implements §3.3), `backend/README.md`, `frontend/README.md`.
- `PLAN.md` cold-start step 7 — dropped the `memory/...` pointer (private memory, not a repo path; was confusing).

### `.claude` settings
- `.claude/settings.local.json` — stale `docker cp x402-adserver-backend:...` allowlist entry updated to `solboards-backend` so the same permission still applies.

### Memory (private to Claude)
- `memory/project_x402_adserver.md` → `memory/project_solboards.md` (renamed file, content updated, MEMORY.md index updated). New rule recorded: "Solboards = product brand, x402 = protocol — keep distinct."

---

## Intentionally NOT renamed

These are real cloud resources or historical records — renaming = data migration, broken history, or just churn for no UX gain:

- **Repo URL** `github.com/heheok/x402-adserver` and **local path** `C:\Development\x402` — kept.
- **GCS bucket** `x402-adserver-creatives` (and the IAM SA `x402-creatives-uploader`, GCP project `x402-494608`) — real, populated, globally-unique cloud resources. References stay in `RUNBOOK.md`, `BUSINESS-CONSTRAINTS.md`, `worklog/planning-2026-04-24.md`, `worklog/session-13.md`.
- **CSS tokens** `--x402-blue` / `--x402-blue-hi` — internal namespace, no UX value to renaming pre-deploy. ~15 files would touch.
- **`design/x-primitives.jsx`** — historical design dump, not part of the React app build.
- **`worklog/session-16.md`** mention of `X402Mark.tsx` — accurate historical record of what was ported in that session; renaming it would falsify history.
- **The x402 *protocol* references** everywhere they actually mean the protocol (`x402-solana`, `createX402Client`, "Signing x402 payment", etc.).

---

## Privy email — what's NOT solved here

Setting `appearance.logo` + `appearance.landingHeader` only themes the **modal**. Privy's OTP login emails pull from the **app name configured in dashboard.privy.io**, which is independent of the SDK config. The user located the rename setting in the Privy dashboard during this session — flipping that there will fix email subjects. No code change needed.

(False alarm: tried exporting a 180×90 wordmark PNG for an "email logo upload" Privy slot — turned out Privy's OTP emails don't use a dashboard-uploaded logo at all, and the modal already has our inline logo. Wordmark export discarded.)

---

## Verification

- `npm run typecheck` → clean (BrandMark rename + import updates compile).
- Final residual scan for `x402[ _-]?[Aa]d[ _-]?[Ss]erver|x402[ _-]?[Aa]dvertiser|X402Mark` (excluding `.git`, `node_modules`, `data`, `.venv`, lockfiles): only intended hits remain (GCS bucket, design dump, worklog history).
- Frontend smoke-tested by user in browser — login modal, dashboard, no regressions.

---

## What's next

Session 18 — GCE deploy. Now that domain + brand are fixed, only open question is **Cloudflare proxy mode**: orange-cloud (proxied, requires DNS-01 with CF API token in Caddy) vs gray-cloud (DNS-only, Caddy does HTTP-01 directly, simpler). Vote: gray-cloud for the hackathon, revisit post-submission if we want CF's edge.
